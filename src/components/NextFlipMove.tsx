import { ReactNode } from "react"
import FlipMove from "react-flip-move"
import styled from "styled-components"

interface NextFlipMoveProps {
  children: ReactNode
}

export const NextFlipMove = styled(FlipMove)<NextFlipMoveProps>``
